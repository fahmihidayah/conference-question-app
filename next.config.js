/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'avatars.githubusercontent.com',
      },
    ],
  },
  experimental: {
    // Enable Sharp for optimized image processing in Docker
    optimizeCss: true,
  },
};

module.exports = nextConfig;
